get:
  tags:
    - Merchant
  summary: Get all skus for a merchant
  operationId: getMerchantSkus
  parameters:
    - $ref: '#/parameters/authHeader'
    - name: id
      in: path
      description: ID of merchant
      required: true
      type: integer
      format: int64
    - name: hydrate
      in: query
      description: >-
        Comma-delimited list of properties to include on model response.
        Available properties: <b>skuProducts</b>
      required: false
      type: string
  responses:
    '200':
      description: success
      schema:
        type: array
        items:
          $ref: '#/definitions/Sku'
    '400':
      description: Invalid ID supplied
    '404':
      description: Merchant not found
