get:
  tags:
    - Merchant
  summary: Get all inventory for a merchant
  operationId: getMerchantInventory
  parameters:
    - $ref: '#/parameters/authHeader'
    - name: id
      in: path
      description: ID of merchant
      required: true
      type: integer
      format: int64
    - name: hydrate
      in: query
      description: >-
        Comma-delimited list of properties to include on model response.
        Available properties: <b>warehouse,product</b>
      required: false
      type: string
  responses:
    '200':
      description: success
      schema:
        type: array
        items:
          $ref: '#/definitions/Inventory'
    '400':
      description: Invalid ID supplied
    '404':
      description: Merchant not found
