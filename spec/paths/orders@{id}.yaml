get:
  tags:
    - orders
  summary: Get Order Details
  description: Optionally you can use your `merchantOrderId` as the `id`
  operationId: getOrder
  parameters:
    - $ref: '#/parameters/authHeader'
    - name: id
      in: path
      description: The FDC order Id
      required: true
      type: string
    - name: merchantId
      in: query
      description: >-
        Optional, if you are using your `merchantOrderId` instead of the FDC
        `id` providing your `merchantId` will speed up your results
      type: integer
  responses:
    '200':
      description: Order Found
      schema:
        $ref: '#/definitions/order'
    '404':
      description: Order not found
delete:
  tags:
    - orders
  summary: Cancel an Order
  operationId: cancelOrder
  parameters:
    - $ref: '#/parameters/authHeader'
    - name: id
      in: path
      description: ID of order that needs to be canceled
      required: true
      type: integer
      format: int64
  responses:
    '200':
      description: Your order was successfully canceled
      schema:
        $ref: '#/definitions/order'
    '404':
      description: Order not found
    '405':
      description: 'Could not cancel your order, perhaps it already shipped'
      schema:
        $ref: '#/definitions/standardError'
