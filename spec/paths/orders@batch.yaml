post:
  tags:
    - Order
  summary: Batch upload orders from csv
  description: >-
    This request must be made as <strong>form-data</strong>. The only accepted
    format is <strong>csv</strong>.
  operationId: batchUploadOrders
  parameters:
    - $ref: '#/parameters/authHeader'
    - name: file
      in: formData
      description: The CSV file containing orders
      required: true
      type: file
  responses:
    '200':
      description: success
