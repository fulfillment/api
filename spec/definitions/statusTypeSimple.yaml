required:
  - status
  - reason
properties:
  status:
    type: object
    required:
      - code
    properties:
      code:
        type: string
        pattern: '^\d{6}$'
  reason:
    description: Human-readable description
    type: string
type: object
