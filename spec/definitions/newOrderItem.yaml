required:
  - sku
  - declaredValue
  - quantity
properties:
  quantity:
    type: integer
  sku:
    type: string
  declaredValue:
    description: A per unit value
    type: number
    format: double
    exclusiveMinimum: true
    minimum: 0
type: object
