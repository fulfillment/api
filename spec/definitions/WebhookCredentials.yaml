required:
  - endpoint
  - environment
properties:
  id:
    description: Unique Webhook Credentials identifier
    type: integer
  endpoint:
    description: Endpoint of the webhook
    type: string
  user:
    $ref: '#/definitions/User'
  password:
    description: Password unique identifier
    type: string
  grantType:
    description: >-
      Grant type: Either Authorization Code, Implicit, Password, Client
      Credentials
    type: string
  token:
    type: string
  environment:
    type: string
  path:
    type: string
  merchants:
    $ref: '#/definitions/Merchant'
type: object
