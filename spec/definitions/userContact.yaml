type: object
required:
  - id
properties:
  id:
    type: integer
  name:
    type: string
  username:
    type: string
  deptLeader:
    type: boolean
  status:
    type: boolean
  updatedBy:
    type: string
    format: date-time
  updatedAt:
    type: string
    format: date-time
  createDate:
    type: string
    format: date-time
  merchant:
    type: object
    properties:
      id:
        type: integer
  contactInfo:
    $ref: '#/definitions/consignee'
  apiKey:
    type: string
    description: Depricated
