type: object
properties:
  data:
    type: array
    items:
      $ref: '#/definitions/itemInventory'
  meta:
    type: object
    properties:
      pagination:
        $ref: '#/definitions/pagination'
