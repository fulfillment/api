properties:
  id:
    type: integer
  merchantSku:
    description: The identifier for a sku provided by the client
    type: string
  isValid:
    description: >-
      Signifies whether the merchantSku provided matches a valid sku in our
      system
    type: boolean
  shouldIgnore:
    description: If this is an ignored sku or a test sku this is true
    type: boolean
  reason:
    description: If an OrderSku is invalid this property provides the reason why
    type: string
  quantity:
    type: integer
  declaredValue:
    description: The value of this item
    type: number
    format: double
  order:
    description: ID of the order
    type: integer
type: object
