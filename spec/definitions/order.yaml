required:
  - id
  - merchantOrderId
  - merchantShippingMethod
  - recordedOn
  - merchant
  - currentStatus
  - originalConsignee
  - validatedConsignee
properties:
  id:
    description: FDC ID for this order
    type: integer
  parentOrder:
    description: Possible reference to `id` if we've grouped orders
    type: integer
  merchantOrderId:
    description: Merchant provided ID
    type: string
  purchaseOrderNum:
    description: 'Merchant provided PO#'
    type: string
  merchantShippingMethod:
    description: Requested ship method
    type: string
  recordedOn:
    description: DateTime order was recorded by FDC
    type: string
    format: date-time
  dispatchDate:
    description: DateTime order was dispatched for fulfillment by FDC
    type: string
    format: date-time
  departDate:
    description: DateTime order departed an FDC warehouse
    type: string
    format: date-time
  merchant:
    $ref: '#/definitions/merchant'
  warehouse:
    $ref: '#/definitions/warehouse'
  currentStatus:
    $ref: '#/definitions/statusEvent'
  originalConsignee:
    $ref: '#/definitions/consignee'
  validatedConsignee:
    $ref: '#/definitions/consignee'
  trackingNumbers:
    items:
      $ref: '#/definitions/trackingNumber'
    type: array
type: object
